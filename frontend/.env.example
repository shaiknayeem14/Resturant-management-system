# Backend API URL (for production Vercel deployment, set this to your deployed backend URL, e.g., https://your-bistro-api.onrender.com/api)
# Leave empty or set to /api if using local Vite proxy
VITE_API_URL=http://localhost:5000/api
